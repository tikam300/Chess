<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chess</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        .chessboard {
            display: grid;
            grid-template-columns: repeat(8, minmax(0, 1fr));
            grid-template-rows: repeat(8, minmax(0, 1fr));
            width: 400px;
            height: 400px;
            transform: rotate(0deg);
        }

        .piece.white {
            color: white !important;
            filter: drop-shadow(0 0 2px rgba(0, 0, 0, 1));
        }

        .piece.black {
            color: black !important;
        }

        .flipped {
            transform: rotate(180deg);
        }

        .square {
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .square.light {
            background-color: #f0d9b5;
        }

        .square.dark {
            background-color: #b58863;
        }

        .piece {
            font-size: 36px;
            cursor: pointer;
        }

        .piece.draggable {
            cursor: grab;
        }

        .dragging {
            opacity: 0.5;
        }

        /* Rotate pieces for black player */
        .flipped .piece {
            transform: rotate(180deg);
        }

        .turn-indicator {
        font-size: 24px;
        font-weight: bold;
        margin: 10px;
        text-align: center;
        }

        .white-turn {
            color: white;
            background-color: #4CAF50; /* Green for white's turn */
        }

        .black-turn {
            color: black;
            background-color: #FFC107; /* Yellow for black's turn */
        }

        .player-role {
            margin: 10px;
            padding: 10px;
            font-size: 18px;
            font-weight: bold;
            text-align: center;
            border: 1px solid #000;
            border-radius: 5px;
        }

        .player-role.white-role {
            background-color: #ffffff;
            color: #000000;
        }

        .player-role.black-role {
            background-color: #000000;
            color: #ffffff;
        }

        .player-role.spectator-role {
            background-color: #f0f0f0;
            color: #000000;
        }

    </style>
</head>
<body>
    <div id="turn-indicator" class="turn-indicator"></div>
    <div id="player-role" class="player-role"></div>
    <div class="w-full flex items-center justify-center h-screen bg-zinc-900">
        <div class="chessboard w-96 h-96 bg-red-800"></div>
    </div>

    <script src="https://cdn.socket.io/4.7.5/socket.io.min.js" integrity="sha384-2huaZvOR9iDzHqslqwpR87isEmrfxqyWOF7hr7BY6KG0+hVKLoEXMPUJw3ynWuhO" crossorigin="anonymous"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/chess.js/0.10.3/chess.min.js" integrity="sha512-xRllwz2gdZciIB+AkEbeq+gVhX8VB8XsfqeFbUh+SzHlN96dEduwtTuVuc2u9EROlmW9+yhRlxjif66ORpsgVA==" crossorigin="anonymous" referrerpolicy="no-referrer"></script>
    <script src="/js/chessgame.js"></script>
</body>
</html>